<%= form_with(model: grupo_user, local: true) do |form| %>
  <% if grupo_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grupo_user.errors.count, "error") %> prohibited this grupo_user from being saved:</h2>

      <ul>
      <% grupo_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :grupo_user_user_id %>
  </div>

  <div class="field">
    <%= form.label :grupo_id %>
    <%= form.number_field :grupo_id, id: :grupo_user_grupo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
